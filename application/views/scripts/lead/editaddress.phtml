<fieldset class="padded_frame blue_frame center">
    <legend>
	<?php 
	    echo isset($this->form->id) ? 
		"Edit address of " : 
		"Add address to "; echo "'".$this->Lead->getFullName()."'"?></legend>
    <?php
    echo $this->form;
    ?>
</fieldset>